; Local development overrides — loaded via: php -c php.ini artisan serve
; Or use: composer serve / composer dev

upload_max_filesize = 25M
post_max_size = 30M
memory_limit = 512M
max_execution_time = 120
max_input_time = 120
